{
    "name": "uoou-solutions-assets",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild assets/bootstrap.js --bundle --outdir=public/build --minify",
        "dev": "esbuild assets/bootstrap.js --bundle --outdir=public/build --sourcemap --watch"
    },
    "dependencies": {
        "bootstrap": "^5.*"
    },
    "devDependencies": {
        "esbuild": "^0.28.0"
    }
}
